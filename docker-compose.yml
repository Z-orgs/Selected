services:
  #  node:
  #    image: node:lts-hydrogen
  #    command: sh -c "npx pnpm install && npm start"
  #    ports:
  #      - 3000:3000
  #    working_dir: /app
  #    volumes:
  #      - ./:/app
  #    environment:
  #      MONGO_URI: mongodb://admin:example@mongo:27017/?retryWrites=true&w=majority
  #      GOOGLE_CLIENT_ID:
  #      GOOGLE_CLIENT_SECRET:
  #      EXPIRES_IN:
  #      CALLBACK_URL:
  #      ADMIN:
  #      PASSWORD:
  #      DEFAULTPASSWORD:
  #      TMPURI:
  mongo:
    image: mongo:focal
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: example
      MONGO_INITDB_DATABASE: selected
    volumes:
      - ./data/mongo:/data/db
